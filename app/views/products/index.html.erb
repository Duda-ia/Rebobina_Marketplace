
  <!-- Destaques section -->
  <section class="section-wrapper destaque-section">
    <h2 class="section-title">DESTAQUES</h2>
    <div class="featured-albums">
      <img src="https://www.udiscovermusic.com/wp-content/uploads/2020/08/Lost-Berlin-820x820.jpg" alt="Ella Fitzgerald album" />
      <img src="https://m.media-amazon.com/images/I/811qGlvATJL._UF1000,1000_QL80_.jpg" alt="The Beatles album" />
    </div>
  </section>
<div>
  <!-- Categories section  -->
  <%= render "shared/categories" %>
</div>

  <div>
    <%= render "shared/novidades" %>
  </div>

  <!-- Big Image & Quote -->
  <section class="section-wrapper split-section">
    <div class="quote-box">
      <div>
        <blockquote>
          “O vinil é uma forma de arte. Você pode pegar o disco, colocar na sua vitrola e escutar a música, mas também pode olhar para o design da capa, ler as letras e realmente se envolver com o que está acontecendo ali. Não é só sobre ouvir a música, é sobre a experiência inteira.”
        </blockquote>
        <div class="author">Jack White — Cantor e Compositor</div>
      </div>
    </div>
  </section>
<main>
  <section class="products-section">
    <div class="products-grid">
      <% @products.each do |product| %>
        <div class="product-card">
          <div class="product-image">
              <% if product.photo.attached? %>
                <%= cl_image_tag product.photo.key, height: 300, width: 400, crop: :fill %>
              <% elsif product.category == "vinil" %>
                <%= image_tag("vinil") %>
              <% elsif product.category == "cd" %>
                <%= image_tag("cd") %>
              <%# <% elsif product.category == "fita cassete" %>
                <%# <%= image_tag("") %>
              <% elsif product.category == "old stuff" %>
                <%= image_tag("radio") %>
              <% end %>
          </div>

          <h3 class="product-title">
            <%= product.name %>
          </h3>
          <p class="product-price">
            <span class="currency">R$</span>
            <%= product.price %>
          </p>
          <%= link_to 'comprar', product_path(product), class: 'button-card' %>
        </div>
      <% end %>
    </div>
  </section>
</main>

</div>
<%#
<div class="container">
  <%# <h1>Products</h1>

  <div id='products'>
    <% @products.each do |product| %>
      <%# <p><%= product.name %>
      <%# <p><%= product.category %>
      <%# <p><%= product.description %>
      <%# <p><%= product.price</p> %>
      <%# <p> %>
        <%# <%= link_to 'Show this product', product %>
      <%# </p> %>
      <%# <% end %>
  <%# </div> %>
<%# </div> %>
