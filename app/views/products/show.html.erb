
<%# <div class="container">
  <p style="color: green"><%= notice %>
  <%# </p> %>
  <%# <%= render @product %>
  <%# <%= link_to "edit", edit_product_path(@product) %>
  <%# <%= button_to "Deletar", product_path(@product), method: :delete, data: { confirm: "Tem certeza?" }%>
<%# </div> %>

<!-- app/views/products/_product.html.erb -->

<!-- app/views/products/show.html.erb -->
<div class="product-card single">
  <!-- Coluna da imagem -->
  <div class="product-image-col">
    <% if product.photo.attached? %>
    <%= cl_image_tag product.photo.key, height: 300, width: 400, crop: :fill %>
      <% elsif product.category == "vinil" %>
      <%= image_tag("vinil") %>
        <% elsif product.category == "cd" %>
          <%= image_tag("cd") %>
          <%# <% elsif product.category == "fita cassete" %>
          <%# <%= image_tag("") %>
          <% elsif product.category == "old stuff" %>
            <%= image_tag("radio") %>
        <% end %>
  </div>

  <!-- Coluna das infos (título, preço, descrição, etc.) -->
  <div class="product-info-col">
    <h3 class="product-title"><%= @product.name %></h3>
    <p class="product-price">
      <strong>Preço:</strong> <%= number_to_currency(@product.price) %>
    </p>
    <p class="product-description"><%= @product.description %></p>

     <%= link_to "Comprar", new_product_order_path(@product), class: 'button-card'  %>

    <!-- Se quiser um campo QTD e um botão comprar:
    <div class="purchase-box">
      <label>QTD:
        <input type="number" min="1" value="1" />
      </label>
      <button>adicionar ao carrinho</button>
    </div>
    -->
  </div>
</div>
